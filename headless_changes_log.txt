HEADLESS HYGIENE ACTIONS LOG
=============================

1. Centralized Detection
   - Created scripts/utils/Headless.gd
   - Implements static func is_headless() checking DisplayServer, Features, and Args.

2. Audio Silence
   - Modified scripts/core/AudioManager.gd
   - _ready(): Returns early if headless.
   - play(), play_fixed(), play_music(): Added guards to return immediately if headless.
   - Prevents "Sound not found" warnings and AudioStreamPlayer creation.

3. UI & Visual Leaks
   - Modified scripts/ui/FloatingText.gd: Returns early in _ready if headless.
   - Modified scripts/debug/PerfTracker.gd: Disables self and overlay in headless.
   - Modified scripts/debug/PerfOverlay.gd: Calls queue_free() in _ready if headless (safety net).

4. Robust Exit Strategy
   - Modified scripts/debug/item_validation/ItemTestRunner.gd
   - _cleanup(): Now explicitly calls queue_free() on:
     * scenario_runner
     * mechanical_oracle
     * status_oracle
     * numeric_oracle
     * report_writer
     * attack_manager
   - Ensures clean ObjectDB on exit.

5. Report Logic
   - Modified scripts/debug/item_validation/ReportWriter.gd
   - Fixed generate_summary_md() to correctly include:
     * Metadata & Header
     * Status Verification Results Table (RESTORED)
     * Top 20 Extreme Deltas (RESTORED)
     * Scope Coverage & Failures (RESTORED)
